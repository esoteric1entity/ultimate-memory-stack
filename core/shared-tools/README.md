# Shared tools

Cross-harness tooling used by every edition and adapter, not scoped to any single one. `lint_runner.py` moved here in v4.0.0 from `core/openclaw-adapter/scripts/` — a deprecate-never-delete compat shim remains at the old path.
